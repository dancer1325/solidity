# how to run locally EACH example?
* ways
  * `solc --bin solidityFilename.sol`
    * print output
  * `solc -o outputDirectory --bin --ast-compact-json --asm sourceFile.sol`
    * output everything | SEPARATE files
